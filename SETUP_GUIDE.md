# Setup Guide

This file is kept as a compatibility pointer. The authoritative documentation for the repository is [README.md](README.md).

Quick start:

```bash
git clone https://github.com/<you>/dotfiles.git ~/.dotfiles
cd ~/.dotfiles
bash install.sh
```

For the full backup, restore, package, security, and migration workflow, read [README.md](README.md).
